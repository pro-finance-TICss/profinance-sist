---
---
<section id="beneficios" class="benefits-section">
  <div class="container">
    <h2 class="section-title">Nuestros <span class="text-gradient-gold">Beneficios</span></h2>
    
    <div class="carousel-container">

      
      <div class="carousel-track">
        <!-- Tarjetas Originales -->
        <div class="card">
          <div class="card-icon">📈</div>
          <h3>Rentabilidad Alta</h3>
          <p>Optimizamos tus recursos para obtener los mejores rendimientos del mercado con estrategias probadas.</p>
        </div>
        
        <div class="card">
          <div class="card-icon">🛡️</div>
          <h3>Seguridad Total</h3>
          <p>Tu patrimonio está protegido con los más altos estándares de ciberseguridad y regulaciones vigentes.</p>
        </div>
        
        <div class="card">
          <div class="card-icon">🤝</div>
          <h3>Asesoría 24/7</h3>
          <p>Expertos financieros a tu disposición en cualquier momento para resolver tus dudas y guiarte.</p>
        </div>

        <div class="card">
          <div class="card-icon">📱</div>
          <h3>Tecnología Punta</h3>
          <p>Plataforma intuitiva y rápida para que gestiones tus finanzas desde cualquier lugar.</p>
        </div>
        <!-- Tarjetas Duplicadas para Bucle Continuo -->
        <div class="card">
          <div class="card-icon">📈</div>
          <h3>Rentabilidad Alta</h3>
          <p>Optimizamos tus recursos para obtener los mejores rendimientos del mercado con estrategias probadas.</p>
        </div>
        
        <div class="card">
          <div class="card-icon">🛡️</div>
          <h3>Seguridad Total</h3>
          <p>Tu patrimonio está protegido con los más altos estándares de ciberseguridad y regulaciones vigentes.</p>
        </div>
        
        <div class="card">
          <div class="card-icon">🤝</div>
          <h3>Asesoría 24/7</h3>
          <p>Expertos financieros a tu disposición en cualquier momento para resolver tus dudas y guiarte.</p>
        </div>

        <div class="card">
          <div class="card-icon">📱</div>
          <h3>Tecnología Punta</h3>
          <p>Plataforma intuitiva y rápida para que gestiones tus finanzas desde cualquier lugar.</p>
        </div>
      </div>
      

    </div>
  </div>
</section>

<script>
  const track = document.querySelector('.carousel-track');

  const section = document.querySelector('.benefits-section');
  
  if (track && section) {
    const cardWidth = 280;
    const gap = 32; // 2rem
    const scrollAmount = cardWidth + gap; 


    // Lógica de Desplazamiento Automático
    let autoScrollInterval: any;

    const startAutoScroll = () => {
      // Limpiar cualquier intervalo existente por seguridad
      clearInterval(autoScrollInterval);
      
      autoScrollInterval = setInterval(() => {
        track.scrollBy({ left: scrollAmount, behavior: 'smooth' });
      }, 2500); // 2.5s para un buen ritmo
    };

    const stopAutoScroll = () => {
      clearInterval(autoScrollInterval);
    };

    // Pausar al pasar el cursor
    section.addEventListener('mouseenter', stopAutoScroll);
    section.addEventListener('mouseleave', startAutoScroll);

    // Inicio Inicial
    startAutoScroll();

    // Bucle continuo de desplazamiento infinito
    track.addEventListener('scroll', () => {
      const maxScroll = track.scrollWidth / 2; // Punto medio exacto debido a la duplicación
      
      // Si llegamos al inicio del conjunto duplicado, volver al inicio real 0
      // Buscamos >= maxScroll - 5 (tolerancia)
      if (track.scrollLeft >= maxScroll - 10) { 
         // Pero, si encajamos exactamente en maxScroll, ¿podría estar a mitad de animación?
         // De hecho, la táctica estándar es: si scrollLeft >= maxScroll, restar maxScroll.
         if (track.scrollLeft >= maxScroll) {
             track.scrollLeft -= maxScroll;
         }
      }
    });
  }
</script>

<style>
  .benefits-section {
    padding: 6rem 0;
    min-height: 80vh; 
    display: flex;
    align-items: center;
    background: linear-gradient(to bottom, transparent, rgba(18, 18, 18, 0.8));
    overflow: hidden; /* Ocultar desbordamiento fuera del contenedor */
  }

  .container {
    width: 90%;
    max-width: 1600px;
    margin: 0 auto;
    text-align: center;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 3rem;
  }

  .carousel-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    position: relative;
  }

  .carousel-track {
    display: flex;
    gap: 2rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding: 2rem 1rem; 
    width: 100%;
    /* max-width eliminado para ocupar todo el espacio permitido */
    
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  
  .carousel-track::-webkit-scrollbar {
    display: none;
  }

  .card {
    flex: 0 0 280px; 
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 2rem;
    text-align: left;
    scroll-snap-align: center;
    transition: all 0.3s ease;
    backdrop-filter: blur(5px);
  }

  .card:hover {
    transform: scale(1.05);
    background: rgba(255, 255, 255, 0.08);
    border-color: var(--color-gold-start);
    box-shadow: 0 10px 40px rgba(255, 215, 0, 0.1);
    z-index: 2;
  }

  .card-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .card h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    color: var(--color-white);
  }

  .card p {
    font-size: 1rem;
    color: #ccc;
    line-height: 1.5;
  }


</style>
